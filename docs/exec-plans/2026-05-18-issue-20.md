# Issue #20: [feat] 공통 레이아웃 로직 — 새 채팅 세션 · 라우터 활성 상태 · a11y

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/20
> Labels: frontend | Milestone: 없음
> 생성일: 2026-05-18

## 목표
- ChatSidebar "새 채팅" 버튼: `POST /api/chat/sessions` → `/chat?sid=<id>` 이동
- NavMenu 활성 탭: 이미 usePathname() 적용 완료 (PR #35)
- a11y: aria-expanded, Escape 키 닫기

## 관련 파일
- `components/ChatSidebar.tsx` — 새 채팅 API 연동 + 로딩/에러 상태
- `components/SidebarLayout.tsx` — Escape 키 닫기, aria-expanded
- `components/NavMenu.tsx` — aria-expanded, Escape 키 드롭다운 닫기
- `app/chat/page.tsx` — useSearchParams로 sid 초기화, Suspense 래핑

## 작업 분해
- [x] `ChatSidebar.tsx`: POST /api/chat/sessions 연동, 로딩·에러 상태
- [x] `ChatSidebar.tsx`: 버튼 disabled + 에러 인라인 표시
- [x] `SidebarLayout.tsx`: Escape 키로 사이드바 닫기, aria-expanded
- [x] `NavMenu.tsx`: aria-expanded, Escape 키로 드롭다운 닫기
- [x] `app/chat/page.tsx`: useSearchParams('sid') → currentSessionId 초기화, Suspense

## 완료 기준
- 새 채팅 버튼 클릭 → API 호출 → /chat?sid=xxx 이동
- API 호출 중 버튼 비활성화
- API 실패 시 사이드바 내 인라인 에러 표시
- Escape 키로 사이드바 / NavMenu 드롭다운 닫힘
- aria-expanded가 열림/닫힘 상태 반영
