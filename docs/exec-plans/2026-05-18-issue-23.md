# Issue #23: [feat] 로그인·회원가입 공통 컴포넌트 분리 (AuthHeader, LoadingSpinner)

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/23
> Labels: frontend | Milestone: 없음
> 생성일: 2026-05-18

## 목표
login/signup 페이지에 중복된 로고 헤더(아이콘+제목+부제) 및 로딩 스피너 SVG를 공통 컴포넌트로 분리한다.

## 관련 파일
- `components/AuthHeader.tsx` — 신규: 아이콘 SVG + title + subtitle props
- `components/LoadingSpinner.tsx` — 신규: animate-spin SVG, size prop
- `app/login/page.tsx` — AuthHeader, LoadingSpinner 컴포넌트로 교체
- `app/signup/page.tsx` — AuthHeader, LoadingSpinner 컴포넌트로 교체

## 작업 분해
- [ ] `components/AuthHeader.tsx` 생성 — title, subtitle props
- [ ] `components/LoadingSpinner.tsx` 생성 — size prop (sm/md/lg)
- [ ] `app/login/page.tsx` — 인라인 헤더·스피너 → 컴포넌트 교체
- [ ] `app/signup/page.tsx` — 인라인 헤더·스피너 → 컴포넌트 교체

## 완료 기준
- AuthHeader: 아이콘 SVG + title + subtitle를 props로 수용
- LoadingSpinner: animate-spin SVG, size prop 지원
- login/signup 페이지에서 중복 인라인 코드 제거 후 컴포넌트 사용
- 기존 UI/동작 변화 없음
