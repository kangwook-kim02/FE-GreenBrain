# Issue #74: [bug] 로그인 후 AppContext 미갱신 — router.push 사용 및 RouteGuard 인증 상태 미체크

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/74
> Labels: bug, frontend | Milestone: 없음
> 생성일: 2026-05-20

## 목표

두 가지 로그인 버그 수정:
1. `router.push('/chat')` → `window.location.href`로 교체 (AppContext 재마운트 보장)
2. RouteGuard에서 로그인 상태 유저의 `/login`·`/signup` 접근 차단

## 관련 파일

- `app/login/page.tsx` — `router.push('/chat')` → `window.location.href = '/chat'`
- `components/RouteGuard.tsx` — `user && isPublic` 조건 시 `/chat` 리다이렉트 추가

## 작업 분해

- [ ] `login/page.tsx`: `router.push('/chat')` → `window.location.href = '/chat'`로 교체
- [ ] `RouteGuard.tsx`: `user && isPublic` 조건 추가 → `/chat` redirect

## 완료 기준

- 최초 로그인 시 `/chat`으로 정상 이동
- 로그인 상태에서 `/login` 접근 시 `/chat`으로 리다이렉트
- 계정 전환 후 AppContext가 새 유저 데이터로 갱신됨
