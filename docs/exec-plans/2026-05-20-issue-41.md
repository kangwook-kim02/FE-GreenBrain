# Issue #41: [feat] 새 채팅 세션 생성 지연(Lazy) 처리

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/41
> Labels: frontend | Milestone: -
> 생성일: 2026-05-20

## 목표
'새 채팅' 클릭 시 즉시 세션을 생성하는 대신, 첫 메시지 전송 시에만 세션을 생성해 빈 세션 누적 방지

## 관련 파일
- `components/ChatSidebar.tsx` — handleNewChat에서 API 호출 제거
- `app/chat/page.tsx` — 세션 생성 후 router.replace로 URL 동기화, useEffect ref 스킵 추가

## 작업 분해
- [ ] ChatSidebar.handleNewChat: API 호출 제거 → router.push('/chat')만 수행, isCreating/createError 제거
- [ ] chat/page.tsx: useRouter import 추가
- [ ] chat/page.tsx: justCreatedSessionRef 추가
- [ ] chat/page.tsx handleSubmit: 세션 생성 후 router.replace('/chat?sid=<id>') + ref 플래그 설정
- [ ] chat/page.tsx useEffect([sid]): ref가 true면 리셋 스킵

## 완료 기준
- '새 채팅' 클릭 시 API 호출 없이 /chat으로 이동
- 첫 메시지 전송 시 URL이 /chat?sid=<id>로 자동 업데이트
- URL 업데이트 시 메시지 목록 초기화되지 않음
- 새 세션이 사이드바에 즉시 반영됨
