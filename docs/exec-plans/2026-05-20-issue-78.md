# Issue #78: [feat] 로그인 후 onboarding_completed 기반 직접 분기 — AppContext bootstrap 의존 제거

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/78
> Labels: frontend | Milestone: 없음
> 생성일: 2026-05-20

## 목표

로그인 응답의 `onboarding_completed` 값으로 즉시 페이지 분기 — `/chat` 로딩 스피너 → `/onboarding` 리다이렉트 중간 과정 제거

## 관련 파일

- `app/login/page.tsx` — 로그인 응답 처리 및 분기 로직 수정
- `.claude/skills/implement-feature/references/api-shapes.md` — 로그인 응답 타입 업데이트

## 작업 분해

- [ ] `login/page.tsx`: 로그인 응답 타입에 `onboarding_completed` 추가
- [ ] `login/page.tsx`: `onboarding_completed: false` → `window.location.href = '/onboarding'`
- [ ] `login/page.tsx`: `onboarding_completed: true` → `window.location.href = '/chat'`
- [ ] `api-shapes.md`: 로그인 응답 shape 업데이트

## 완료 기준

- 로그인 성공 시 `onboarding_completed: false`면 `/onboarding`으로 즉시 이동
- 로그인 성공 시 `onboarding_completed: true`면 `/chat`으로 즉시 이동
- `/chat` 경유 없이 바로 목적지로 이동
