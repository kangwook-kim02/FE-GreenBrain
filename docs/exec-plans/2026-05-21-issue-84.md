# Issue #84: 모바일 사이드바 — 하단 NavMenu 제거, 햄버거 토글로 전체화면 사이드바 전환

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/84
> Labels: design, frontend | Milestone: -
> 생성일: 2026-05-21

## 목표
하단 NavMenu(탭바)를 제거하고 PC와 동일한 햄버거 토글 버튼을 모바일에도 노출한다.
모바일에서 토글 클릭 시 ChatSidebar가 전체화면 오버레이로 표시된다.

## 관련 파일
- `components/SidebarLayout.tsx` — 토글 버튼 가시성, content 하단 패딩 제거, 초기 사이드바 상태
- `components/ChatSidebar.tsx` — 모바일 full-screen 오버레이 CSS, 세션 선택 시 닫기
- `app/chat/page.tsx` — NavMenu import·사용 제거

## 작업 분해
- [ ] SidebarLayout: 초기 상태 `false` + useEffect로 데스크탑에서만 열기
- [ ] SidebarLayout: 토글 버튼 `hidden sm:flex` → `flex` (모바일에도 표시)
- [ ] SidebarLayout: content wrapper `pb-16 sm:pb-0` → `pb-0` 제거
- [ ] ChatSidebar: aside className — 모바일 open: `fixed inset-y-0 left-0 z-50 w-full`, 데스크탑: 기존 유지
- [ ] ChatSidebar: inner div `w-64` → `w-full sm:w-64`
- [ ] ChatSidebar: 세션 선택·새 채팅 시 모바일에서 onClose() 호출
- [ ] app/chat/page.tsx: NavMenu 제거

## 완료 기준
- 모바일에서 하단 탭바가 사라지고 채팅 헤더에 햄버거 버튼이 표시됨
- 햄버거 버튼 클릭 시 ChatSidebar가 전체화면으로 표시됨
- 사이드바에서 채팅 항목 선택 시 사이드바가 닫히고 채팅 화면으로 복귀
- 데스크탑 동작은 기존과 동일
