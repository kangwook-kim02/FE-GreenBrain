# Issue #30: [feat] 채팅 세션 히스토리 관리 — 목록 조회 · 세션 로드 · 삭제

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/30
> Labels: frontend | Milestone: 없음
> 생성일: 2026-05-18

## 목표
- ChatSidebar: mock 데이터 → 실제 `GET /api/chat/sessions` 연동, 날짜 그룹화
- 세션 클릭 → `/chat?sid=<id>` 이동 + chat page에서 메시지 로드
- 세션 삭제: Optimistic UI (`DELETE /api/chat/sessions/{id}`)
- 활성 세션 하이라이트

## 관련 파일
- `components/ChatSidebar.tsx` — 세션 목록 API 연동, 삭제, 하이라이트
- `app/chat/page.tsx` — sid 변경 시 `GET /api/chat/sessions/{id}/messages` 로드

## 작업 분해
- [ ] `ChatSidebar.tsx`: GET /api/chat/sessions 연동, 날짜 그룹 함수
- [ ] `ChatSidebar.tsx`: 로딩 스켈레톤 (다크 테마)
- [ ] `ChatSidebar.tsx`: 에러 인라인 표시
- [ ] `ChatSidebar.tsx`: 세션 클릭 → router.push('/chat?sid=<id>')
- [ ] `ChatSidebar.tsx`: 삭제 버튼 hover 표시 + Optimistic DELETE
- [ ] `ChatSidebar.tsx`: 활성 세션 하이라이트 (useSearchParams)
- [ ] `app/chat/page.tsx`: sid 변경 시 메시지 로드 (GET messages)
- [ ] `app/chat/page.tsx`: 히스토리 메시지 → Message[] 매핑

## 완료 기준
- 사이드바 마운트 시 실제 세션 목록 표시 (오늘/어제/이전 7일)
- 세션 클릭 시 해당 세션 메시지가 채팅 화면에 로드됨
- 활성 세션 강조 표시
- 세션 삭제 시 즉시 목록에서 제거 (실패 시 복구)
- 로딩 중 스켈레톤, 에러 시 인라인 메시지
