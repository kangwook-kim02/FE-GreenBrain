---
issue: 9
title: "[feat] 채팅 토큰 상태 처리 — 색상 전환 · 소진 처리"
url: https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/9
date: 2026-05-15
---

## 목표
TokenBar 색상 전환·소진 UI는 PR #24(issue #3)에서 이미 구현 완료.
이슈 #9의 남은 작업: mock 토큰을 제거하고 `GET /api/tokens/today` API로 초기화, AppContext.tokens 연동.

## 관련 파일
- `app/chat/page.tsx` — mockTokens 제거, AppContext tokens 사용, 마운트 시 GET /api/tokens/today 호출
- `components/TokenBar.tsx` — 색상 전환 이미 구현 완료 (변경 불필요)

## 작업 분해
- [x] TokenBar 색상 전환 (>50%→green, 20~50%→amber, <20%→red) — PR #24에서 완료
- [x] 토큰 소진 화면 UI — PR #24에서 완료
- [x] 입력창 remaining <= 0 시 비활성화 — PR #24에서 완료
- [ ] chat/page.tsx: mockTokens 제거 → AppContext tokens 사용
- [ ] chat/page.tsx: 마운트 시 GET /api/tokens/today → updateRemainingTokens 호출
- [ ] 로딩 상태 (토큰 초기화 중) 처리

## 완료 기준
- 클라이언트 자체 토큰 계산 없음 — API 응답 tokens_remaining 기준
- 채팅 페이지 마운트 시 실제 잔여 토큰 표시
- 소진(remaining === 0) 시 입력 비활성화 + 소진 화면 전환
