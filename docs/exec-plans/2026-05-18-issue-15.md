# Issue #15: [feat] 피드 API 연동 — feed 목록 · like · delete

> https://github.com/GreenBrain-Inha/FE_GreenBrain/issues/15
> Labels: frontend | Milestone: 없음
> 생성일: 2026-05-18

## 목표
챌린지 피드 목록을 mock → 실제 API로 전환하고, 좋아요·삭제 기능을 API 연동

## API 엔드포인트 (GreenBrain-API.md 기준)
- `GET /api/challenges/feed?limit=20&offset=0` — 피드 목록 조회
- `POST /api/challenge-photos/{photo_id}/like` — 좋아요
- `DELETE /api/challenge-photos/{photo_id}` — 게시물 삭제

## 관련 파일
- `app/challenges/feed/page.tsx` — 메인 피드 페이지 (mock → API 전환)
- `lib/api.ts` — apiFetch 유틸
- `contexts/AppContext.tsx` — user.id 필요 (isOwner 판별)
- `components/SkeletonCard.tsx` — 로딩 스켈레톤
- `components/EmptyState.tsx` — 빈 상태

## 작업 분해
- [ ] FeedItem 인터페이스를 API 응답 shape에 맞게 변경
- [ ] MOCK_FEED 제거 → GET /api/challenges/feed useEffect 호출
- [ ] 로딩 상태: SkeletonCard 3개 표시
- [ ] 에러 상태: 인라인 에러 메시지 + 재시도 버튼
- [ ] 좋아요: POST /api/challenge-photos/{photo_id}/like + Optimistic UI
- [ ] 삭제: DELETE /api/challenge-photos/{photo_id} + 목록에서 즉시 제거
- [ ] isOwner: user_id === currentUser.id 비교
- [ ] 빈 상태: items.length === 0 → 안내 메시지 + 챌린지 참여 버튼

## 완료 기준
- 로딩 / 에러 / 빈 상태 3종 구현
- Optimistic UI 좋아요 (실패 시 롤백)
- 삭제 성공 시 목록 즉시 제거
- API 응답 shape과 타입 일치
